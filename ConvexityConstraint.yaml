name: ConvexityConstraint
channels:
  - conda-forge
dependencies:
 - python=3.6
 - cgal=4.9 # Compatibility was broken since original code in 2015
 - nlopt=2.4.2 # Somehow works better for my application
 - eigen
